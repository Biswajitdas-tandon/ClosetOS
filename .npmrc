# Default pnpm isolation (web prefers this for Next.js + RSC).
# The mobile app overrides this with its own .npmrc that sets node-linker=hoisted,
# because Metro/RN cannot resolve pnpm's symlinked layout.
auto-install-peers=true
strict-peer-dependencies=false
